import {createIconSetFromFontello} from 'react-native-vector-icons';
import fontelloConfig from '../../../config/zmicons.json';

export const Icon = createIconSetFromFontello(fontelloConfig, 'zmicons');
